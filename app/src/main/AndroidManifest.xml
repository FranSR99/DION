<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".MyAppointments" />
        <activity android:name=".BookDoctor" />
        <activity android:name=".BuyPharmacy2" />
        <activity android:name=".DeliveryStatePharmacy" />
        <activity android:name=".ReceiptScan" />
        <activity android:name=".BuyPharmacy" />
        <activity android:name=".MyPharmacy" />
        <activity android:name=".MyHospital" />
        <activity android:name=".MainActivity" />
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>